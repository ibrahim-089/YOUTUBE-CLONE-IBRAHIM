const menu = document.querySelector('#menu');

const sidebar = document.querySelector('.side-bar');

menu.addEventListener('click', function(){
    side-bar.classList.toggle('show-sidebar');
});
